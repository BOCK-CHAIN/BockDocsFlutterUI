<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow HTTP connections for development -->
    <!-- In production, remove this and use HTTPS only -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    
    <!-- Optional: Allow specific domains for development -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.2.2</domain> <!-- Android emulator localhost -->
        <domain includeSubdomains="true">192.168.1.0</domain> <!-- Common local network range -->
        <domain includeSubdomains="true">192.168.0.0</domain> <!-- Common local network range -->
        <domain includeSubdomains="true">172.16.0.0</domain> <!-- Common local network range -->
    </domain-config>
</network-security-config>


